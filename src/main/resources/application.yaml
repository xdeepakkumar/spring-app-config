server:
  port: 9296

spring:
  application:
    name: CONFIG_SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/xdeepakkumar/spring-app-config
          clone-on-start: true
